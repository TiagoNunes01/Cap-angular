import { MultipicaPorPipe } from './multipica-por.pipe';

describe('MultipicaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipicaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
